defaults:
  - _self_

dataset: data/ruler/unlabeled
limit:
output_dir: outputs/eda_annotate/eda
save:
  excel: ${output_dir}/eda_stats.xlsx
  json: ${output_dir}/eda_summary.json
anomalies:
  enabled: true
  max_entries: 12
  area:
    iqr_multiplier: 1.5
    min: 65536
    max:

hydra:
  run:
    dir: .
  output_subdir: null
